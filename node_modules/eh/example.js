'use strict';

const Eh = require('./').Eh;

const t = new Eh(function(resolve, reject) {
  setTimeout(resolve, 1000, 'resolved');

  return function() {
    reject('halted');
  };
});

t.haltAfter(Math.random() * 1500);
t.promise.then(console.log, console.error);
